image: alpine:latest

stages:
  - build

build:
  stage: build
  script:
    - apk add build-base
    - apk add cmake
    - apk add ncurses-dev
    - mkdir build
    - cd build
    - cmake ..
    - cmake --build .
  artifacts:
    paths:
      - ./build/detectwin
    expire_in: 1 week
